# CANONICAL-DOCS.toml — what this repo publishes.
#
# The keep-list IS the interface: what reaches a public reader is decided here and
# nowhere else. A file that is not declared and sits under a doc root is dropped
# silently, so anything a published document links to belongs in this list.
#
# The audience is someone OUTSIDE this ecosystem who wants to check an implementation
# against the protocol. `requirements/` is the thing they want; the suites are how we
# show the requirements are unambiguous enough to be implemented twice.
#
# Non-prose files (`.diag`, `.cbor`, source, configs) are never dropped by the filter,
# so the requirement corpus, the suites and the pinned snapshots publish as code. What
# is declared below is the PROSE that explains them.

[project]
name   = "Entity System Conformance"
slug   = "entity-system-conformance"
repo   = "entity-system-conformance"
github = "https://github.com/EntityChurch/entity-system-conformance"
intro  = """
The independent conformance instrument for the Entity Core Protocol: a neutral statement
of what the protocol requires, and several independently built test suites over it. A
single oracle cannot distinguish "the peer is wrong" from "the oracle is wrong" — its own
errors are invisible by construction — so this repo separates the obligation from the
probe and implements the obligations more than once, in different languages, by different
authors, without them reading each other. Where the suites agree, the requirement said so;
where they disagree, the disagreement is the product, and it is always exactly one of three
things: a specification gap, an implementation bug, or a bad check. Early and honest about
it: 50 requirements authored, 15 of the core floor's 98 obligations covered, 28 requirements
executed against real peers, two suites. The numbers this repo publishes about itself are
ratcheted and are lowered by doing the work, never by editing the number.
"""

# ── What each directory IS ────────────────────────────────────────────────────────
# Declared rather than inferred from a path name. Longest path wins.

[[area]]
path      = "docs/status"
kind      = "status"       # dated, episodic, ages out. Written for the next session, not a reader
publishes = false

[[area]]
path      = "docs/outbox"
kind      = "outbox"       # packets we have SENT. Routing is internal and never publishes
publishes = false

[[area]]
path      = "docs/archive"
kind      = "archive"      # dated BY DESIGN and does not age out; closed docs keep their date
publishes = false

[[area]]
path      = "docs/adr"
kind      = "adr"          # this repo's own decisions. Cited by number from anywhere; not published
publishes = false

[[area]]
path      = "docs/agents/memory"
kind      = "memory"       # durable, indexed, edited in place — see the note on `internal` below
publishes = false

# ── Living documents — durable, rewritten in place, and not all of them publish ────

[[living]]
path = "docs/STATUS.md"    # the rolling status. Declared as a [[doc]] below: this one DOES publish

[[living]]
glob     = "docs/status/TRACKER-*.md"
internal = true            # durable AND must never publish: what is open with one counterpart

[[living]]
glob     = "docs/agents/memory/*.md"
internal = true
# ⚠ WHY MEMORY IS INTERNAL HERE, stated because the standard's example publishes it and we do not.
# These files are a straight MOVE out of AGENTS.md — the standard's own instruction, and the right
# one — and they have not been rewritten for an outside reader. They still carry finding ids,
# packet stems, sibling-repo paths and the names of internal gates, which this repo's publication
# rule says do not belong in a declared document. Publishing them unscrubbed would leak ecosystem
# operations; DELETING the material to make them publishable is the exact mistake the standard
# opens by naming. So they are declared and held internal, and the scrub is the next pass: a file
# that becomes a clean read for a stranger moves to [[doc]] and publishes. The half of this
# material that IS written for an outside reader already publishes, as docs/PROCESS.md.

# ── Overview ──────────────────────────────────────────────────────────────────────

[[doc]]
path  = "README.md"
title = "entity-system-conformance"
blurb = "What the instrument is, the three-way question it makes decidable, and where it stands."
group = "Overview"

[[doc]]
path  = "docs/STATUS.md"
title = "Status"
blurb = "The rolling log: what is measured, what is open, and what is deliberately not claimed."
group = "Overview"

[[doc]]
path  = "CHANGELOG.md"
title = "Changelog"
blurb = "What has landed, in Keep a Changelog form."
group = "Overview"

[[doc]]
path  = "docs/CLI.md"
title = "Command-line tools"
blurb = "Every make target — build, test, lint, run against peers, compare two instruments — and the suite executable's own flags."
group = "Tools"

# ── Working in this repository ────────────────────────────────────────────────────
# ⛔ THESE ARE LOOSE TOP-LEVEL PROSE AND ARE DELETED FROM THE PUBLIC TREE IF UNDECLARED.
# That is how a repository ends up public with no AGENTS.md without anyone deleting one. They are
# also the first four files a contributor — human or agent — opens, so they belong in the
# published set on their own merits, not only to satisfy the floor.

[[doc]]
path  = "AGENTS.md"
title = "Working in this repository"
blurb = "What this seat is for, the three prohibitions that protect it, the rules a change is held to, and where to look next. Read first, by anyone."
group = "Working here"

[[doc]]
path  = "CLAUDE.md"
title = "Agent entry point"
blurb = "A shim. Guidance lives in AGENTS.md and AGENTS-STANDARD.md; this file only points at them, so there is one source of truth rather than two that drift."
group = "Working here"

[[doc]]
path  = "AGENTS-STANDARD.md"
title = "Shared conventions across the project"
blurb = "The conventions every repository in this project shares — history, build interface, contribution, honesty about conformance numbers. Identical wherever it appears."
group = "Working here"

[[doc]]
path  = "METHODOLOGY.md"
title = "Methodology — disciplines, doctrines and the ratchet"
blurb = "How a rule earns the right to exist here, and how what a piece of work taught gets fed back. Carried in every repository; opened when something points at it, not read up front."
group = "Working here"

# ── How the work is done ──────────────────────────────────────────────────────────
# The method is most of the deliverable here: an instrument is only as good as the
# argument for trusting it, so the argument publishes too.

[[doc]]
path  = "docs/PROCESS.md"
title = "How this repository works"
blurb = "The three prohibitions, the loop, how a rule earns the right to exist, the inputs that are declared as data, and an honest account of where the work has got to."
group = "Process"

[[doc]]
path  = "docs/DISCIPLINE-CHARTER.md"
title = "The disciplines"
blurb = "This repository's own invariants, each earned on a second incident and each with an enforcement point someone can run."
group = "Process"

# ── The deliverable ───────────────────────────────────────────────────────────────

[[doc]]
path  = "requirements/README.md"
title = "The requirement corpus"
blurb = "What a requirement file carries, the three states of an id, and the rules each file is held to."
group = "The requirements"

[[doc]]
path  = "docs/DESIGN-THE-REQUIREMENT-FORMAT.md"
title = "The requirement format"
blurb = "How one obligation is written so someone who has never seen a reference oracle can implement a check for it."
group = "The requirements"

[[doc]]
path  = "spec-data/README.md"
title = "Pinned specification snapshots"
blurb = "Why the spec is pinned rather than tracked, and what a snapshot is a snapshot of."
group = "The requirements"

[[doc]]
path  = "spec-data/entity-core-protocol/PROVENANCE.md"
title = "Vendored specification text — provenance and licence"
blurb = "The protocol text under spec-data/ is another project's work, copied unchanged and not claimed here: where it comes from, its terms, and how to verify the copy."
group = "The requirements"

# ── Building and running a suite ──────────────────────────────────────────────────

[[doc]]
path  = "docs/DESIGN-THE-SUITE-CONTRACT.md"
title = "The suite contract"
blurb = "How a suite is invoked and what it emits — the interface a consumer swaps one instrument for another across."
group = "The suites"

[[doc]]
path  = "docs/WORKFLOW-SUITE-BRINGUP.md"
title = "Standing up a suite"
blurb = "The ten stages end to end, each graded by whether it has ever caught a real defect."
group = "The suites"

[[doc]]
path  = "docs/BRIEF-SUITE-2.md"
title = "The brief a new suite author is handed"
blurb = "What an independent author may read, what they must not, and why the second suite is worth its cost only if built blind."
group = "The suites"

[[doc]]
path  = "suites/py-prototype/README.md"
title = "Suite 1 — py-prototype"
blurb = "Python, standard library only: what each module was written from, and the hazards its language forced it to refuse."
group = "The suites"

[[doc]]
path  = "suites/rs-conformance/README.md"
title = "Suite 2 — rs-conformance"
blurb = "Rust, std only, zero dependencies: what it was built from, what it was not allowed to read, and what it found."
group = "The suites"

# ── How the whole thing is put together ───────────────────────────────────────────

[[doc]]
path  = "docs/MODEL-THE-MEASUREMENT-STACK.md"
title = "The measurement stack"
blurb = "Who measures whom, why a peer count flatters every cohort, and where the regress of checking the checker actually stops."
group = "Architecture"
