49
# Requirement files still in the PRE-ADR-0003 shape — obligation and probe fused in one file.
#
# A RATCHET, exactly like UNEXECUTED-CEILING and SOURCES-CITATION-DEBT. `make lint` counts the
# legacy-shape files every run and FAILS if the count rises. Lowered by splitting a file; NEVER
# raised. Reaching 0 retires this file and the gate's legacy branch with it.
#
# WHY THE CORPUS IS MIXED ON PURPOSE, rather than converted in one pass. SPECIFICATION-FORMAT §8.5a:
# "Converting an inventory is a shape change and nothing else [MUST]. Where converting one surfaces a
# requirement that is really two, or one no check can reach, THAT IS A FINDING TO FILE, not a thing
# to fix inside a formatting edit." A 50-file mechanical pass cannot file findings, because nobody
# stopped to notice one. Per-file is slower and it is the only way the rule is satisfiable.
#
# 49 at 2026-09-15 (b). Split so far:
#   ECP-R57  -> suites/py-prototype/items/entity-core-protocol/ECP-R57-P1.diag
#              (re-authored against v0.8.2.25 in the same pass, because its obligation INVERTED —
#               F75. Splitting it without re-authoring would have moved a wrong requirement into a
#               new shape and called it progress.)
